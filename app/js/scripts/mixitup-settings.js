export function mixitupSettings() {
  // === ACTIVATE MIXITUP PLUGIN IN INDEX.HTML ===
  var mixer = mixitup(".gallery__inner", {
    load: {
      filter: ".cat-living-room",
    },
  });
  // === / ACTIVATE MIXITUP PLUGIN IN INDEX.HTML ===
}